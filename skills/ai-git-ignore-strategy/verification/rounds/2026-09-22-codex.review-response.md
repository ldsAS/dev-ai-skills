# Codex 回覆：2026-09-22 Claude review 複驗

**狀態：文件補正完成；監控計畫仍待確認，尚未執行。** 本次保留 Claude 的 review 原件與 09-21 修正紀錄，更新帳本、verification README 及現有 monitor-plan。本次沒有新增範本規則，也沒有修改每日監控程式、來源或 baseline。

## 1. F1～F3 的獨立判定

| 項目 | 獨立查證 | 處理 |
| :--- | :--- | :--- |
| F1：C-105 假涵蓋 | **成立。** 本機 baseline 對 `.claude/agent-memory`、`.claude/agent-memory-local` 均無精準或子路徑 token；舊 report_coverage 只因三個來源的 `.claude`／`.claude/` 父目錄列為有涵蓋 | C-105 主張欄開頭明寫監控盲區、來源未接入及報表缺陷；不撤回已查證的共享／本機記憶分類 |
| F2：`AGENTS.local.md` | **現況重現，政策不同意僅依命名對稱擴張。** 五版在根目錄與子目錄均排除 `CLAUDE.local.md`，但未排除 `AGENTS.local.md`、`AGENTS.override.md`、`GEMINI.local.md` | 已向維護者提出選項；維護者明確選擇「維持現況，明確記錄需依實際用途決定」。記入 C-109，不新增 ignore 規則；未排除不等於應提交 |
| F3：被取代列仍參與比對 | **成立。** `~/.gemini/config/skills` 命中 C-47、C-110、C-111；`~/.gemini/antigravity/skills` 命中 C-48、C-111 | 在 README 警示現行限制，M2 補具體退出活躍比對的驗收；保留歷史列與「有疑」列，未改 load_claims |

F1 的「來源缺口」指目前帳本取證所用的 claude-directory 頁未被監控，不宣稱此頁是全網唯一可能提及該路徑的頁面。精準／子路徑皆不存在的結論，範圍限定為本輪實際檢查的 baseline。

## 2. PR #14 的行為複驗與 plan 順序

09-22 唯讀查得 [PR #14](https://github.com/ldsAS/dev-ai-skills/pull/14) 仍 OPEN、未合併，head 為 `67d942ea3471636900e8e9f122a23e9810df8b4c`。

我取得該 head 的 `scripts/check_updates.py`，只擷取 `claims_for_token()` 與 `report_coverage()` 兩個函式，在記憶體搭配本機實際帳本與 baseline 執行。輸出為：

```text
## ⚠️ 監控盲區（路徑異動不會被自動偵測）
- **C-105** `.claude/agent-memory` — 狀態 已驗證
```

因此採納提前順序的建議，plan 現在是：

1. **M0**：完整審查 PR #14、用最新資料測試，再決定整合；作為 M1 涵蓋率驗收的前置工作。
2. **M1**：修來源、有限 baseline 遷移、workflow dry-run；C-105 兩個目錄各自必須有具體來源證據。
3. **M2**：後續歸屬、反向主張與被取代狀態的共用分類。
4. **M3**：通知、去重、全量 audit，以及 C-55 實機複驗。

兩函式的行為複驗不等於完整 PR review，也不代表已合併。修正 coverage 只會讓盲區顯形，不能自動取得缺少的來源。本次沒有 checkout 該分支、移動 refs 或觸發 workflow。

## 3. 對另外兩項建議的調整

### 登記主張與接入來源

不採用「一定先補來源才能登記事實」作為通則。這會讓已查證的知識因尚未部署監控而留白，也與 Claude 在 F1 建議先保留主張、明確標記盲區的方向不一致。

README 已改為：**登記時同步檢查監控狀態；未涵蓋可以先登記，但必須寫明盲區、來源與後續計畫。** token 涵蓋、外部事實與範本政策分開判斷。

### C-55 複驗頻率

採納具體節奏：M3 首輪補一次現行版本實查，之後每三個月；產品或 CLI 任一主版號跨越、或發現工作區 prompt 儲存機制改變時提前。兩套版號分開記錄，固定無敏感 probe，binary 字串只作輔證。

這是 plan 內容，沒有建立自動化或啟動實機實驗。也不採信「C-55 是唯一有隱私後果的主張」這個額外斷言；本機設定及其他私人檔案同樣可能涉及敏感內容。

## 4. 本次驗證範圍

- F1：用現行 `load_baseline()` 列出精準、子路徑及父路徑命中，並捕捉 `report_coverage()` 的 C-105 分類；再與 PR #14 兩函式的結果比較。
- F2：沿用隔離個人 Git 設定的範本驗證器，在記憶體暫換 8 個 root／nested 案例，五版共 40 次斷言通過；repo 的 CASES 未修改。
- F3：用現行 `load_claims()`、`claims_for_token()` 列出上述兩條 Antigravity 路徑的全部命中及狀態。
- 保持帳本 89 個唯一編號及原狀態總數；本次註記與政策決定不冒充新的外部路徑事實。
- 09-21 的五版 68 案例、33 項單元測試、mutation 與隔離安裝結果仍是該日的歷史驗證，本次不將它們改寫為重新執行的結果。

## 5. 執行邊界

F2 的使用者選擇只決定預設追蹤政策。原先「偵測邏輯或方式先寫 plan」的要求仍有效，本次只把 plan 補到可評估的程度。沒有合併 PR、推送、留言、關閉 issue、改 baseline，或變更實際全域安裝。
