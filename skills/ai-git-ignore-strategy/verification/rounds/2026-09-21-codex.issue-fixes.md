# 2026-09-21 Codex 修正紀錄：Claude 範本缺口與監控待辦

**狀態：本機修正與驗證完成，尚未提交／推送或部署。** 監控來源與偵測方式依使用者要求先列 [plan](./2026-09-21-codex.monitor-plan.md)，本輪未實作。GitHub #15、#16 仍未結案。

## 1. 範圍與依據

- 基於 Claude 2026-09-18 交接，獨立讀取 GitHub #15／#16 最新留言與 Actions，再用監控自己的 `fetch()`、`normalize()`、`context_for()` 讀官方原文。
- 本機起點 `ae7c6d6`；遠端 main `5883f03`，差異只有一次 bot baseline commit。本輪不修改 baseline，不把本機舊快照推回遠端。
- 五版為 antigravity、claude、codex、generic、vscode，均同步同一組政策；原有工具專屬內容保留。
- 保留 Claude 原交接檔；未修改安裝器、實際全域技能、其他專案的 `.gitignore` 或 GitHub issue／PR。

## 2. 已完成的修正

| 項目 | 修正與邊界 | 帳本 |
| :--- | :--- | :--- |
| `.claude/CLAUDE.md` | 精準放行團隊指令；同名 `.bak` 不放行 | C-103 |
| `.claude/hooks/` | 預設放行團隊設定所需的腳本；審查可執行內容與敏感資訊，私人 hook 另列精準排除；`.env`、日誌仍被原規則擋下 | C-104 |
| `.claude/agent-memory/` | 放行官方定位為共享的 project 記憶，提交前仍審查內容 | C-105 |
| `.claude/agent-memory-local/` | 使用 `**/.claude/agent-memory-local/`，保護根目錄與子目錄；不由 nested 案例推論 runtime 一定產生 nested 目錄 | C-105 |
| `CLAUDE.local.md` | 不含斜線的排除規則涵蓋所有層級；`CLAUDE.local.md.example`、共享指令仍放行 | C-106 |
| `.claude/scheduled_tasks.json` | 登記為與執行情境相關的狀態；根目錄既有規則已排除，只補回歸案例 | C-107 |
| `~/.claude/skills/.trash/` | 家目錄同步技能回收區，只補帳本 | C-108 |
| `.claude/AGENTS.md` | 納入 09-19 新訊號，精準放行；說明 v2.1.277 起的條件式支援與功能限制 | C-109 |

Q1 採預設放行團隊 hook 腳本，理由是共享設定的依賴也要能提交。官方範例證明位置與用途；「預設放行」是本 skill 的政策，不是「這個目錄內任何私人腳本都應提交」的外部事實。

Q2 採預設放行 project 記憶，因官方明確定位為團隊共享，且提供 `memory: local` 作為本機替代位置。這不跳過原本逐檔閱讀、敏感內容審查與專案追蹤決定。

## 3. Antigravity 證據處理

- C-44 補官方 hooks.md 的工作區位置佐證；五版移除過時的「尚未直接驗證位置」註解，但維持 `# !.agents/hooks.json` 的專案決策邊界。
- C-47 原列保留，標為被取代→C-110。現行 CLI skills 文件與歷史相關子系統 changelog 不一致，C-110 保留「有疑」；未做新版 runtime 實驗，安裝器不變。
- C-48 原列保留，標為被取代→C-111。現行 IDE 文件列 `config/skills/` 且仍支援 legacy `antigravity/skills/`；只確認文件，不推論同名技能 precedence。
- C-100～C-102 留給 PR #14，不重編。帳本現有 89 列：已驗證 78、結構性 4、移出範圍 3、被取代 3、有疑 1。未把尚未合併的三列計入。

## 4. 實際驗證

| 驗證 | 結果 |
| :--- | :--- |
| 先增加案例、尚未修範本 | 五版各有 8 個失敗，明確重現漏排除與誤排除 |
| `python -B -X utf8 scripts/verify_gitignore_template.py` | 五版各 68 案例全過，共 340 次行為斷言 |
| `python -B -X utf8 -m unittest discover -s tests` | 33 項通過；現有 `check_updates.py:300` 仍有檔案未關閉 ResourceWarning，未混入本輪監控修改 |
| 在記憶體逐一移除六條新規則 | 每版 6/6 mutation 被指定案例抓到；不改 repo 檔案 |
| 用測試用 global excludes 預先排除 `CLAUDE.local.md`，再移除範本該規則 | 根目錄與 nested 案例仍轉紅，證明個人全域規則不會掩蓋範本缺口 |
| skill-creator 的 `quick_validate.py` | 五版全過 |
| 原始 `install.ps1 auto`，Windows PowerShell 5.1，暫存 USERPROFILE | 預載系統 `Microsoft.PowerShell.Utility` 後 exit 0；六個安裝目標的 SHA256 均等於來源，見下 |

範本驗證器新增 17 個案例，並在暫存 repo 中隔離 system／global Git 設定與 `core.excludesFile`。這是測試環境隔離，不改遠端每日路徑偵測。Git exit 0／1／其他錯誤的三態判讀維持不變。

Mutation 對象依次為 `!.claude/CLAUDE.md`、`!.claude/AGENTS.md`、`!.claude/hooks/`、`!.claude/agent-memory/`、`**/.claude/agent-memory-local/`、`CLAUDE.local.md`；移除一條時只測該條的行為案例，避免把不相關失敗算成保護有效。

### 安裝測試範圍

在子程序的 `USERPROFILE` 指向暫存 fixture，先建立各工具偵測目錄及兩個 legacy 技能標記，執行未修改的安裝器 auto 模式，再逐一用 Python SHA256 比對：

| 暫存目標 | variant | SHA256 前 16 碼 |
| :--- | :--- | :--- |
| `.claude/skills/` | claude | `c39d907838e3496b` |
| `.gemini/config/skills/` | antigravity | `6aaab278f153a069` |
| `.gemini/antigravity/skills/` | antigravity | `6aaab278f153a069` |
| `.agents/skills/` | generic | `e40d41499ba722f8` |
| `.codex/skills/` | codex | `2071684397edcc1c` |
| `.copilot/skills/` | vscode | `d085096ae49812fe` |

首次直接用 `powershell -NoProfile -File install.ps1 auto` 因本測試環境未自動載入 `Get-FileHash` 而失敗；獨立檢查確認系統 Utility module 存在，顯式 `Import-Module Microsoft.PowerShell.Utility` 後重跑通過。**不宣稱裸命令通過，也不以 PowerShell 測試代替 Linux install.sh 實測。** 所有寫入、替換、清理均限制在已驗證的暫存絕對路徑；使用者現有全域技能未更新。

## 5. 尚未解除的 GitHub 提醒

- #16：失效來源仍待 plan 的 M1 實作與部署；單靠本輪範本修正不會恢復排程綠燈。
- #15：已分類並修正本輪確認的範本問題，但 CLI 路徑仍待實機，且本機變更尚未部署，不能現在結案。
- PR #14：尚未合併，其偵測歸屬修正不在本輪自動套用。
- 不觸發現有遠端 workflow 做測試：分支 dispatch 目前仍可能發通知、關 issue 與推 baseline，先在 plan 中設計 dry-run 邊界。
