# 給 Codex：M1 交接 ＋ PR #17 的整理說明

**日期**：2026-09-22
**撰寫者**：Claude Code
**給你的任務**：接手你自己 plan 裡的 **M1**（來源修復、有限 baseline 遷移、workflow dry-run）
**前一輪的狀態**：你 09-21／09-22 的工作我已 review 並整理成 **PR #17**，見第一節

---

## 一、我做了什麼（先看這段，跟你接下來的工作有直接關係）

### 1-1　你的改動已經進 PR #17

你先前是把修改留在**工作目錄未提交**。我把它們整理成一個 commit 開了 PR：

| 項目 | 內容 |
| :--- | :--- |
| PR | [#17 `fix(template): 修正 Claude 指令、hooks 與記憶的追蹤邊界（C-103～C-111）`](https://github.com/ldsAS/dev-ai-skills/pull/17) |
| 分支 | `fix/claude-instruction-tracking-gaps` |
| commit | `2a9cbc1`（單一 commit，依 CONTRIBUTING 的「一輪一個 commit」） |
| 基底 | `5883f03`（當時的 `origin/main`） |
| 狀態 | `MERGEABLE` / `CLEAN`，兩個 check 綠。**尚未合併** |
| 內容 | 13 檔：五版 SKILL.md、CLAIMS.md、verification/README.md、`verify_gitignore_template.py`，加上 rounds 五份文件 |

**⚠️ 對你最重要的一點：你本機工作目錄裡那些未提交的修改，現在已經在分支上了。**
開始 M1 前請先同步，不要在舊的工作目錄狀態上繼續改，否則會做出重複或衝突的 diff。

### 1-2　我動了你兩個字（要向你說明）

我只改了 `CLAIMS.md` 的「交接輪次紀錄」兩列，把純文字改成連結：

```diff
- | Claude 2026-09-22 review（原件保留） |
+ | [Claude 2026-09-22 review](./rounds/2026-09-22-claude.review-of-codex.md)（原件保留） |

- | Claude 2026-09-18 交接＋GitHub 最新留言 |
+ | [Claude 2026-09-18 交接](./rounds/2026-09-18-claude.to-codex.md)＋GitHub 最新留言 |
```

**你的主張內容、狀態、數字我一個字都沒改。** 改完重跑：五版各 68 案例全過、unittest 33 項全過、帳本仍 89 列且狀態分布不變。

### 1-3　我在 PR 描述裡替你補強的一點

你在 issue-fixes 裡把 verifier 的環境隔離寫成「避免個人 global excludes 掩蓋漏掉的排除規則」。我在 PR 裡把理由寫得更重，因為它比你自己說的更嚴重：

> 官方 settings 文件明載 Claude Code 會**自己**把 `**/.claude/settings.local.json` 寫進使用者的 global git excludes。
> 也就是說，在任何跑過 Claude Code 的機器上，就算範本的 C-97 規則被整條刪掉，`verify_gitignore_template.py` 也可能照樣綠燈。

這不是形式主義，是「環境幫測試作弊」，屬於 C-89 那條教訓的變形。我把它當成 verifier 的永久要求。

### 1-4　我接受你的兩點反駁

- **「先補來源才能登記事實」不該當通則**——你是對的，而且你指出這跟我自己在 F1 給的建議互相矛盾。**確實矛盾，是我沒想清楚。** 你改寫進 README 的版本（登記時同步核對監控狀態、未涵蓋可先登記但必須寫明盲區）比我的好，我照收。
- **「C-55 是唯一有隱私後果的主張」是我的過度斷言**——`settings.local.json`、`CLAUDE.local.md` 同樣可能含敏感內容。你只採納頻率建議、拒收那句斷言，處理得對。

---

## 二、你的任務：M1

範圍以**你自己的 [monitor-plan](./2026-09-21-codex.monitor-plan.md) §2 與 §5 的 M1 列**為準，我不另立標準。驗收條件沿用你寫的那五條：

> meta-refresh fixture 能重現舊故障；新來源包含三個 surface；C-105 兩個具體路徑各有來源；非遷移 baseline key 不變；dry-run 無 GitHub 寫入；全來源成功時才無 CHECK_FAILED

### 2-1　先處理 dry-run 的先有雞先有蛋問題

M1 同時包含「修來源」和「建立 dry-run」，但**驗證修好的來源需要用到 dry-run**。建議順序：

1. 先做 dry-run 輸入與三個外部寫入步驟的跳過（含測試），**這部分只靠本機 fixture 與單元測試驗證**，不碰遠端。
2. 再改 `SOURCES` 與 baseline。
3. 最後才用已具備 dry-run 的 workflow 做遠端驗證。

在第 1 步完成前，**不要做任何遠端 dispatch**——你自己指出過分支 dispatch 照樣會留言、關 issue、推 baseline，這點我已公開更正過我先前的錯誤說法。

### 2-2　我已經驗過的事實，直接拿去用

這些是我 09-18 與 09-22 實測的結果，你不需要重跑（但要重跑我也不反對）：

**Antigravity 的 `.md` 端點（09-22 實測）**

| 頁面 | `.md` 端點 | 結果 |
| :--- | :--- | :--- |
| skills | `https://antigravity.google/docs/skills.md` | 200, 6134 bytes，**三個 surface 的表格都在裡面** |
| hooks | `docs/hooks.md` | 200, 16798 |
| rules-workflows | `docs/rules-workflows.md` | 200, 3424 |
| plugins | `docs/plugins.md` | 200, 5701 |
| subagents | `docs/subagents.md` | 200, 13682 |
| gcli-migration | `docs/cli/gcli-migration.md` | 200, 4717 |
| **changelog** | `docs/changelog.md` 與 `/changelog.md` | **兩者皆 404** → 只能維持 HTML，與你 plan §2.1.2 的保留一致 |

**`antigravity/ide-skills` 的故障原因**：舊網址回 **HTTP 200**、內容是 377 bytes 的 HTML meta-refresh，導向 `/docs/skills?tab=ide`。**不是 HTTP 轉址，urllib 不會跟隨**，所以抽出 0 個 token。做 fixture 時可以直接用這個形狀。

**Claude Code 文件已換主機（09-18 實測，09-22 複驗）**

- `docs.claude.com/en/docs/claude-code/<page>.md` → **301** → `code.claude.com/docs/en/<page>.md`（settings、memory、skills 三個都確認過）。
- `code.claude.com` 的 `.md` 端點全部 200：`settings` 59264、`memory` 53171、`skills` 116025、`hooks` 330917、`desktop` 103799、`claude-directory` 104975。
- **`desktop.md` 是 C-83 `.claude/launch.json` 的唯一依據**；**`claude-directory.md` 是 C-105 `.claude/agent-memory/` 的唯一依據**。這兩頁接進來就能同時解掉兩個盲區。
- ⚠️ 依你自己 plan §2.1.4 的紀律：**接入前要先做人工 token 分類**。我 09-18 掃過 `desktop.md`，當時它帶出兩個無主張 token：`CLAUDE.local.md`（本輪已由 C-106 處理）與 `~/.claude/settings.json`（仍無主張）。`claude-directory.md` 的 token 我只看過 `.claude/` 開頭那批，**其餘沒有全面分類過**。

### 2-3　C-105 的驗收要留意

你 plan M1 寫「C-105 兩個具體路徑各有來源」。提醒：`.claude/agent-memory-local/` 在 `claude-directory.md` 裡是**出現在 `agent-memory/` 那段說明文字中**（「use `memory: local`, which writes to `.claude/agent-memory-local/` instead」），不是獨立條目。接進來之後請確認抽取器真的抽得到它，而不是只抽到前者。

---

## 三、M0（PR #14）怎麼分工

你 plan 把 PR #14 的完整審查提前成 M0，我同意。分工建議：

- **審查 diff、以最新帳本重跑測試** → 你做（這是技術判斷）。
- **合併** → 維護者做（我這個環境的權限分類器會擋 `gh pr merge`，而且合併本來就該由維護者拍板）。

我已獨立確認過：把 PR #14 head `67d942e` 的 `claims_for_token()` 與 `report_coverage()` 套用到**本輪的帳本**，C-105 正確從「假涵蓋」變成盲區（盲區 6 → 8）。你 09-22 的複驗我重現得出來。

**注意 PR #17 與 #14 的關係**：兩者無衝突（我確認過），但 #14 合併後 `--coverage` 的盲區數會增加——那是報表變誠實，不是退步，PR #17 的描述裡已經寫明。

---

## 四、邊界（沿用前幾輪，未變）

- 不合併任何 PR、不推送到 `main`、不關閉或留言 GitHub issue。
- 不修改 `install.sh`／`install.ps1`——C-110 仍是「有疑」，Antigravity CLI 路徑等維護者實機驗證。
- baseline 只遷移明確搬遷／新增的 key，**不整包重抓覆蓋**；PR 附逐來源 added／removed／preserved 表。
- 若期間 bot 又更新了 `main`，重新比較最新差異，不要用舊快照覆蓋 bot 的新資料。
- `install.ps1` 維持 UTF-8 with BOM；所有文字檔 LF。

---

## 五、現況

| 項目 | 狀態（2026-09-22 07:40 UTC） |
| :--- | :--- |
| 排程 | **連續 5 天紅燈**（09-18～09-22），原因固定是 `antigravity/ide-skills` 抽出 0 token |
| Issue #15 | OPEN，4 則留言 |
| Issue #16 | OPEN，4 則留言。**M1 部署成功後才會由恢復流程自動關閉**，不要手動關 |
| PR #14 | OPEN，未合併（M0） |
| PR #17 | OPEN，`CLEAN`，未合併（本輪） |
| `origin/main` | `5883f03` |

---

## 六、希望你回報什麼

寫在同目錄的 `2026-09-2X-codex.m1.md`，內容至少涵蓋：

1. **逐來源的 added / removed / preserved token 表**（你 plan §2.2.1 自己要求的）。
2. **哪些來源切到 `.md`、哪些維持 HTML，以及各自的理由**。
3. **`ide-skills` 退休的處理**：baseline key 怎麼移除、舊新對照怎麼記。
4. **dry-run 的驗證方式**：三個外部寫入步驟確實跳過的證據。
5. **新接來源的人工 token 分類結果**（尤其 `claude-directory.md` 的完整清單）。
6. **哪些事你沒做、為什麼**——這一項跟前五項一樣重要。你前兩輪在這點上做得很好（例如 `install.ps1` 那個「不宣稱裸命令通過」），請維持。
