# 給 Codex：2026-09-23 部署後巡查與分支清理紀錄

**日期**：2026-09-23
**撰寫者**：Claude Code
**狀態**：巡查完成；分支清理與 issue #12 連結修正**已執行**（經維護者同意）。**main 沒有新增提交**，本檔未提交。

---

## 零、你問的那件事：分支清理有沒有遺漏未合併的改動

**沒有。** 刪除前我做了以下檢查，你可以直接重跑（指令見第五節）：

| 檢查 | 結果 |
| :--- | :--- |
| 仍開著的 PR | 0 個（#11、#14、#17、#18 皆已合併） |
| 三條一般合併分支 `origin/main..origin/<branch>` | `codex/m1-monitor-sources`、`fix/claude-instruction-tracking-gaps`、`fix/issue-12-cross-source-triage` 皆 **0 個** |
| `feat/ai-summary-in-check-updates` | `rev-list` 顯示 **3 個**（`debaef3` 功能、`43772bf` PROBE、`93d2238` revert）——但 PR #11 是 **squash 合併**成 `36394c7`（單一 parent）。**`git diff --stat 36394c7 origin/feat/ai-summary-in-check-updates` 整棵樹為空**，內容完全相同 |
| 本地分支是否有遠端沒有的 commit | 四條皆 **0 個** |
| 本機獨有 `ci/path-based-update-check` | 無遠端；`origin/main..` 為 **0 個** |

刪除結果：

| 分支 | 遠端 | 本地 | 刪除時的 tip |
| :--- | :--- | :--- | :--- |
| `codex/m1-monitor-sources` | 刪 | 刪（`-d`） | `0a3a1ba` |
| `fix/claude-instruction-tracking-gaps` | 刪 | 刪（`-d`） | `2a9cbc1` |
| `fix/issue-12-cross-source-triage` | 刪 | 刪（`-d`） | `67d942e` |
| `feat/ai-summary-in-check-updates` | 刪 | 刪（**`-D`**，git 判定未合併；依據是上表的整棵樹比對） | `93d2238` |
| `ci/path-based-update-check` | — | 刪（`-d`） | `e408521` |

**復原保險**：`refs/pull/11/head`（`93d2238`）、`refs/pull/14/head`（`67d942e`）、`refs/pull/17/head`（`2a9cbc1`）、`refs/pull/18/head`（`0a3a1ba`）在遠端都還在，PR 頁面也可一鍵 Restore branch。

`delete_branch_on_merge` 維持 `false`（維護者決定不開）。

---

## 一、同時做的另一件事：修正 issue #12 的兩條失效連結

我先前在 #12 留言裡用**分支網址**連到 rounds 紀錄，其中一條在 PR #13 合併、分支被刪時就已經 404。刪分支前先修：

| 留言 ID | 原本 | 現在 |
| :--- | :--- | :--- |
| `5616888190` | `blob/fix/issue-12-lexical-false-positive/…/2026-09-10-claude.candidate-triage.md`（已 404） | `blob/main/…` → 200 |
| `5632948108` | `blob/fix/issue-12-cross-source-triage/…/2026-09-11-claude.cross-source-triage.md` | `blob/main/…` → 200 |

只替換網址的分支段，兩則長度差正好等於分支名縮成 `main` 的字數（35→4、32→4）。原文已備份。repo 內文件都用相對路徑，不受影響。

**這是我的錯，往後發到 GitHub 的連結一律指向 `main` 或固定 commit。**

---

## 二、部署後巡查結果（main `403a3f5`）

| 項目 | 結果 |
| :--- | :--- |
| `unittest`（含 PyYAML） | 55 項全過 |
| 五版範本 | 各 68 案例全過 |
| `--coverage` | 59 涵蓋／6 盲區／65。**C-83、C-105 已移出盲區**。剩下的 C-24、C-78、C-41、C-42、C-43 為反向主張；真正的缺口只剩 C-55 |
| 你 10:10 的 dry-run dispatch（run `35847273249`） | 通知、恢復關閉、baseline 提交三步**全部 skipped**；#15、#16 在 01:35 後無新留言 → 無副作用 |
| workflow 寫入 guard | 三步皆要求 `dry_run == 'false'`，fail-closed，確認 |
| 本機 live dry-run | baseline SHA 前後相同。`copilot/repo-instructions` 一次 `WinError 10054`，**單獨重試即成功、token 零變動**——是我這台機器對 docs.github.com 的連線被重設，不是機制問題；你在 runner 上的 dry-run 沒有失敗 |

### 你抓到的 P2 是我的 bug

PR #14 的 C-101 用 `new_sources` 判斷「此路徑目前仍出現在」，而 `new_sources` 含失敗來源沿用的舊 baseline。更糟的是我當時在帳本把它**當成設計寫進去**（「查 `new_sources`，含抓取失敗而保留舊基準者」）。你改查 `verified_sources` 並補逐來源失敗的回歸與 mutation，處理得對。

---

## 三、明天（09-24 約 01:35 UTC）排程的預測

這是新版機制第一次正式執行。本機 dry-run 預告會報 **`copilot/hooks` 頁改版**，我已先分類：

| Token | 判定 |
| :--- | :--- |
| ✅ `.claude`、`settings.local.json` | **有意義**：VS Code Copilot 現可讀 Claude hooks——「A `settings.json` or `settings.local.json` file in a `.claude` folder — Requires `chat.useClaudeHooks`」。範本不需改（實測 `.claude/settings.json` 放行、`.claude/settings.local.json` 擋），但 **C-104 的審查警語更重要**：`.claude/hooks/` 的腳本現在可能被兩個工具執行。建議補進 C-02／C-03／C-104 的佐證 |
| ✅ `.github/hooks/log-tool-use.cjs`、`project-context.cjs`、`require-approval.cjs` | 範例腳本，實測放行 |
| ✅ `.github/hooks/tool-use.log` | 範例 hook 的輸出日誌，被既有 `*.log` 擋下（實測） |
| ❌ `.github/hooks/audit.log`、`format.json`、`formatting.json`、`security.json` | 範例改名。**不會歸屬到任何主張**，因為 C-76 的路徑欄是 glob `.github/hooks/*.json`——M2 的已知項目 |

預期結果：

- **#16 應自動關閉**——前提是 28 個來源全部抓取成功。
- **#15 會多第 6 則留言**（M3 未做，異動仍沿用同一張 issue）。

---

## 四、建議的下一步

1. 明天確認 #16 是否自動關閉——新機制的第一個正式驗收點。
2. 把第三節 Copilot 讀 Claude hooks 這一點補進帳本（純佐證，不改範本）。
3. **M3 優先於 M2**：#15 的堆積每天都在發生，是目前唯一持續累積的問題。
4. C-110（Antigravity CLI 全域路徑）的實機驗證仍待維護者。

---

## 五、重現指令

```bash
git fetch origin --prune
git branch -r                                   # 預期只剩 origin/main
gh pr list --state open                         # 預期為空
git diff --stat 36394c7 93d2238                 # squash 分支 vs squash commit，預期為空
for n in 11 14 17 18; do git ls-remote origin "refs/pull/$n/head"; done
gh api repos/ldsAS/dev-ai-skills/issues/12/comments --paginate --jq '.[].body' | grep -cE "blob/(fix|feat|codex|ci)/"   # 預期 0
```
