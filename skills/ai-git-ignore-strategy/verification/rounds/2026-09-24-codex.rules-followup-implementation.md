# 2026-09-24 Rules 修復實作與驗收

**狀態：本機實作與驗收完成，待維護者安排 Claude review；尚未 commit／push。** 2026-09-24 本輪完成，最後整合檢查於 07:56 UTC（15:56 台北）通過。依 [定稿計畫及 R1～R5 裁定](./2026-09-24-codex.rules-followup-plan.md) 執行；遠端 CI／正式告警恢復仍待發布後驗證。

起點：已唯讀核對遠端 main 為 `f10d4ca2bbad177a437f30bd4294378feb242a64`，將本機 main 快轉後建立 `codex/rules-source-followup`。原有計畫與三份 Claude 原稿保留。本次不修改 workflow／installer／token 抽取與比對算法。

## 1. Review 裁定

- R1：加入 rules.json 分享警語；`../` 的解析基點及團隊目錄可用性須核對，不把相對路徑自動判為 repo 外。私人絕對路徑為內容審查條件，不宣稱已驗證 rules.json 的任意絕對路徑語意。
- R2：新增四條 prompt 回歸案例，且額外啟用 skills、plugins、agents 白名單，檢查一般檔案可提交而 prompt 檔仍被擋。
- R3：把 rules 頁 CLI rules／plugins 位置列為 C-110 相關資料點；兩者不是新 token，也不能代替 skills discovery 探針。
- R4：已排 M3-a 來源搬頁診斷。Markdown 404 與成功取得 meta-refresh 是兩個情境；本次沒有實作替代 HTML 探測或自動跟隨。
- R5：本輪目標完成實作及本機驗收；以下記錄實際結果，遠端正式恢復獨立追蹤。

## 2. 本次來源快照與遷移分類

直接透過 repo 的 `fetch()`、`normalize()`、`extract_tokens()` 重抓，與同日稍早獨立取證內容相同。hash 是解壓後文字的 UTF-8 SHA-256。

| 來源 | HTTP／token | SHA-256 |
| :--- | :--- | :--- |
| [Rules](https://antigravity.google/docs/rules.md) | 200；9,500 bytes；17 個原始 token | `b3689bbde57d1304a0b582356c9fed43124f7579b224f9048db0b9de08adb6a0` |
| [IDE Workflows](https://antigravity.google/docs/ide/workflows.md) | 200；2,181 bytes；0 token，是真實文件 | `b344f3b804d1e0f6e8c329775ef59ff64406c4cb28e574015a4f7eeb3d75cd69` |
| 舊 rules-workflows HTML | 200；337 bytes；0 token，meta-refresh → `/docs/rules` | `0bd3273c4edefd7240cabc45dd7db6b24249f3d48708ffb3c75298083d1c02fe` |
| 舊 rules-workflows.md | 404 | 無成功正文 |

新 entry 的 17 個原始 token 分類如下。保留拼法，不因本文分類而改動抽取或比對方法。

| Token | 判定與處置 |
| :--- | :--- |
| `.agents/rules.json` | 正式登記位置與共用設定；C-115，精準放行並審查引用路徑 |
| `/.agents/AGENTS.md` | 文件 `<dir>` 佔位符後的路徑，非 OS 根目錄；C-115 |
| `/.agents/GEMINI.md` | 同上；C-115 |
| `.agents/rules/` | 既有共享規則目錄；C-57，維持 |
| `/.agents/rules/` | 目錄層範例的前導分隔字元；同 C-57，不新增不同規則 |
| `/.agent/rules/` | 舊佈局相容敘述的另一拼法；同 C-57 |
| `.agents/rules/typescript.md` | 平坦目錄範例，不是工具強制建立的檔名 |
| `.agents/rules/frontend/react.md` | 巢狀目錄範例，需登記的語意已列 C-115 |
| `AGENTS.md` | 既有規則檔名，多 scope；不由裸 token 推定根目錄限制 |
| `GEMINI.md` | 同上，既有規則檔名 |
| `~/.gemini/AGENTS.md` | 文件全域位置；不新增 repo ignore 規則，未驗證 runtime |
| `~/.gemini/GEMINI.md` | 舊 baseline 已有的全域位置，維持 |
| `~/.gemini/config/AGENTS.md` | 文件全域位置；不新增 repo ignore 規則，未驗證 runtime |
| `~/.gemini/config/GEMINI.md` | 同上 |
| `~/.gemini/config/rules/` | 文件全域模組規則位置；不據此推定 skills 探索位置 |
| `~/.gemini/antigravity-cli/rules/` | 舊 baseline 已有；補 C-110 相關資料點，保留未驗狀態 |
| `~/.gemini/antigravity-cli/plugins/` | 舊 baseline 已有；同上，相關 plugins changelog 不能代替 skills runtime |

舊 entry 共 7 個：`.agent/rules`、`.agents/rules`、`.agents/rules/`、`.agents/rules/testing-style.md`、`~/.gemini/GEMINI.md`、`~/.gemini/antigravity-cli/plugins/`、`~/.gemini/antigravity-cli/rules/`。

其中 `.agents/rules` 與 `.agents/rules/` 為既有尾斜線別名；`.agent/rules` 對應新頁的 `/.agent/rules/` 拼法，抽取器照樣保留原文。`testing-style.md` 的消失是例子被替換，不能判成 rules 機制刪除。新舊 entry 在人工完成上述分類後才遷移。

Workflows 頁記載在 2026-11-01 前改用 skills 的規劃；沒有可識別 token 不代表沒有內容或機制消失。本輪人工複閱並登記語意盲區，未加入假的零 token 監控來源。

## 3. 實作範圍

- monitor 只有 SOURCES 一行變更：`antigravity/rules-workflows` → `antigravity/rules`，使用 `rules.md`；沒有調整空頁門檻、比較、歸屬、coverage 或失敗策略。
- 五版同步三檔白名單、R1 分享警語、巢狀 prompt 防護及 config 註解式 opt-in；C-43 的否定觀察補版本限制。`AGENTS.local.md` 政策不變。
- 帳本新增 C-115（規則）、C-116（config）、C-117（防禦政策），共 98 條：84 已驗證、6 被取代、4 結構性、3 移出範圍、1 有疑。C-110 維持有疑；C-77／C-104 限定 Local harness 的 hooks 相容範圍。
- `tests/test_monitor_sources.py` 加入精準路徑及 404／redirect 寫回保護；`tests/test_gitignore_opt_ins.py` 驗證真正啟用白名單後的邊界；verifier 保留全部既有 68 案，再加入 22 案，共 90 案／版。
- 更新 verification README 與舊計畫的現況導引；Claude 三份原稿保持 byte-identical。

## 4. 驗收結果

| 驗證 | 結果 |
| :--- | :--- |
| `python -X utf8 -m unittest discover -s tests -q` | **59 tests，OK，exit 0**；含新來源及 opt-in 測試 |
| `python -X utf8 scripts/verify_gitignore_template.py` | **五版各 90 案全部通過，共 450 案，exit 0** |
| config opt-in | 每版 5 案：config／settings 允許，tmp／cache／config 備份排除，共 25 案 |
| R2 啟用白名單 | 每版 8 案：rules／skills／plugins／agents 下的一般檔允許，四個 prompt 檔排除，共 40 案 |
| 撤回測試（只改記憶體範本） | 移除三條精準白名單：每版 3 案失敗；移除新 prompt 排除：每版 4 案失敗，共 **35 個預期失敗** |
| 404／redirect 保護 | 成功來源刻意新增 token 以強制暫存 baseline 寫回；失敗 rules entry 完整保留、不報消失，其他 entry／版本精準比對通過 |
| live `--dry-run` | **28 個來源、361 個 token，全數與 baseline 相符，exit 0**；無 CHECK_FAILED／UPDATE_DETECTED／BASELINE_CHANGED 訊號 |
| `--coverage` | **63 條涵蓋／5 條盲區／68 條有路徑主張**；僅表示現行算法的 token 命中，限制見下文 |
| skill-creator `quick_validate.py` | 五版 frontmatter／結構檢查全部 exit 0 |
| Windows installer auto | 隔離 USERPROFILE；6 個預期 target 全部安裝，6 份 SKILL.md 與 source hash 一致；exit 0 |
| 文件／diff | 本地連結、98 個唯一 ID／狀態計數及 `git diff --check` 通過；五版新增四條規則與分享警語一致，原有工具區段順序及註解差異保留 |

測試中原有 `open(...).read()` 產生的 ResourceWarning 仍存在（`check_updates.py` 及 template verifier）；未隱藏，也未擴大修改 monitor。第一次測試揭露合成 fixture 漏了裸 `.agents/rules/`，已補回。回歸測試以三個必要精準檔案為斷言，不把完整 live baseline 固定等於 fixture，以免未來正常新增 token 就使測試失效；修正後全套 59 項通過。

### baseline 範圍與 hash

基底是 `f10d4ca`，只退役舊 key、新增新 key；其餘 **27 個 source entries、schema 2 與四項版本完全不變**。保留的版本：Claude 2.1.281、Codex 0.156.1、Gemini CLI 0.61.0、Antigravity 2.17.0。

- 遷移前工作檔 SHA-256：`9fb1f22aa9fcbedbbc379ab52081b4b2ea71ded050e82621fb4f7b0c5511425d`。
- 遷移後 SHA-256：`7c07ab1fbc6b45833d04b33a7a9c21ec5f0d8bd3e1ff608ef13a3e59d8e2c4da`。
- live dry-run 前後均為遷移後 hash，沒有寫回；跨行尾環境比對基底時應另做 JSON 語意比較。

### coverage 的限制

新來源的三個精準路徑都命中 C-115；config 命中 C-116。五條列出的盲區為 C-24、C-78、C-43、C-55、C-117。**C-41／C-42 歷史列也被新來源命中，不能解讀為過去的「不存在」又獲證實。** M2 的狀態／scope 改善尚未實作；C-117 是防禦政策，C-110 則是文件與 runtime 未釐清，不能只看 token 覆蓋數字判為完成。

### 隔離安裝

在暫存 USERPROFILE 先建立受檢工具與兩個 legacy 目錄，執行未修改的 `install.ps1 -Mode auto`。執行前核對每個可能被替換的目的路徑都在暫存根目錄內；安裝後逐份比對 SHA-256，未改真實全域技能。

| 目的路徑（相對隔離 USERPROFILE） | source variant | SHA-256 |
| :--- | :--- | :--- |
| `.claude/skills/ai-git-ignore-strategy/SKILL.md` | claude | `72bca026c483a2991e6c9c37f90d76e9f89ac0f13db3c2154377882ddb49b436` |
| `.gemini/config/skills/ai-git-ignore-strategy/SKILL.md` | antigravity | `eaa6308370aff77efeb973a6328c95fc30cc6462e1c9b4680cb7321de267d246` |
| `.gemini/antigravity/skills/ai-git-ignore-strategy/SKILL.md` | antigravity | 同上 |
| `.agents/skills/ai-git-ignore-strategy/SKILL.md` | generic | `8546c56e37f5d938a9bd058361b7bf57b9d7753bf296b8f7022a5bf89d10098a` |
| `.codex/skills/ai-git-ignore-strategy/SKILL.md` | codex | `91140d584c1c35a3e7c3e9a1affb407844e61324b097e91883c4a7950be2dcc9` |
| `.copilot/skills/ai-git-ignore-strategy/SKILL.md` | vscode | `9a6637034e8042fddae8ad06ed2d41a7b9be03ec4ba7ce6e4202ce586f7ff704` |

這是 Windows 安裝與內容驗證，不是 Linux installer 或 runtime 的探索／優先序驗證。

## 5. 遠端狀態與 Claude review 入口

本輪開始查得開啟 issue 仍為 #15、#16，最後更新 2026-09-24 01:37:06 UTC。正式故障最後證據為 [35943630343](https://github.com/ldsAS/dev-ai-skills/actions/runs/35943630343)。本批未發布，不能宣稱 #16 已恢復；待 review／發布後，再核對正式排程的全部來源成功及恢復結案步驟。

Claude 可從本檔、修訂計畫與工作樹 diff 開始；**新測試、fixture、計畫和紀錄仍是 untracked，單看 `git diff` 會漏掉，需一併看 `git status --short` 所列檔案**。本機來源已恢復，遠端仍未部署；M2、M3-a、M3 及 C-110 runtime 探針留待後續。

可重現命令（repo 根目錄，逐條檢查退出碼）：

```powershell
python -X utf8 -m unittest discover -s tests -v
python -X utf8 scripts/verify_gitignore_template.py
python -X utf8 scripts/check_updates.py --coverage
python -X utf8 scripts/check_updates.py --dry-run
git diff --check
git status --short
```
