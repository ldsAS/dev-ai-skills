# 2026-09-23 M1 推送與 M0 待修項收尾

## 執行計畫（先於程式改動）

維護者本輪要求先 push，再繼續修正未完成項目。M1 已提交並推送至
`codex/m1-monitor-sources`，commit `20fa93d58d4903cd1ae810964cc1140ce88113b3`；
本機與遠端一致。此分支以 PR #17 為基底，本輪接續處理前次回報的 M0 P2 與整合衝突。

1. 在目前分支本機整合 PR #14 的 `67d942ea3471636900e8e9f122a23e9810df8b4c`，
   保留其 commit ancestry。手動解決 CLAIMS.md 衝突，保留 C-100～C-114、新舊取證與取代關係，
   重算狀態數量；README 更新目前涵蓋率，歷史快照仍保留。
2. 將本輪成功抽取的來源另外記錄。跨來源「目前仍出現」只能查這個集合，不能查含失敗舊資料的
   baseline payload。抓取失敗與空集合保護仍保留各自舊 baseline 並送出 CHECK_FAILED，
   不抑制原本的新增／消失訊號，也不改寫入及版本門檻。
3. 先加入反例：另一來源網路失敗、另一來源回空殼，以及失敗和成功來源同時存在；確認修正前轉紅、
   修正後通過。保留成功來源仍存在的正例，並以回退至舊查找集合的 mutation 證明斷言有效。
4. 跑完整測試、五版範本、精準涵蓋率、live dry-run 與 baseline 不寫入核對，提交並推送同一分支，
   檢查遠端 CI。這裡的本機整合不等於將 PR 合併到遠端 main。

本輪不擴大為 M2／M3 的 schema、通知重設或 runtime 實驗。那些是另列的後續階段；
使用者本輪所指的前次待修 P2 與 PR 整合衝突在本輪完成。

## 實作與驗收

### 已完成的改動

- 本機 merge PR #14；CLAIMS.md 的唯一衝突採人工整合，保留兩輪完整主張和原始輪次文件。
  主張 ID 無重複：95 列＝83 已驗證＋4 結構性＋3 移出範圍＋4 被取代＋1 有疑。
- `new_sources` 仍負責下次 baseline；新增 `verified_sources` 僅納入本輪成功且通過空集合保護的來源。
  「目前仍存在」改查後者；新增項的「本次之前存在」仍查歷史 baseline。偵測及寫入門檻不變。
- 測試 fixture 支援逐來源拋出例外；新增網路失敗、空殼、成功／失敗混合三項回歸，包含正式及 dry-run 模式。
- README 更新目前涵蓋率為 59 涵蓋／6 盲區／65 有路徑主張，明示被取代、反向與跨工具語意仍是 M2 限制。
  C-105 兩路徑及 C-83 的精準來源不受影響；AGENTS.local.md 政策不變。

### 驗證

| 項目 | 結果 |
| :--- | :--- |
| 先補測試再修程式 | 修正前 5 個情境失敗；修正後完整 55 項通過 |
| 反向 mutation | 只把 removed 查找改回 `new_sources`，原 5 情境再度轉紅，0 個測試錯誤 |
| baseline 保留 | 失敗來源 entry 保留；正式模式成功來源照常更新；dry-run 完整 bytes 不變 |
| 五版範本 | 每版 68 案例通過；M0 沒有改 SKILL.md、安裝器或 baseline |
| 本機 live dry-run | 28 個來源均未失敗；抓到下節兩筆新增訊號，正常回報 UPDATE_DETECTED；沒有 CHECK_FAILED |
| baseline SHA-256 | dry-run 前後同為 `cc3eaf5616ca36e4f2b56e952ac7262797ebcd0431a891ef728b92cf220b7ea5` |
| M1 已推送版本 CI | [monitor 35766525389](https://github.com/ldsAS/dev-ai-skills/actions/runs/35766525389) 與 [template 35766525277](https://github.com/ldsAS/dev-ai-skills/actions/runs/35766525277) 均成功（20fa93d） |
| 格式與合併 | diff --check 通過；無衝突標記；本次提交保留 PR #14、#17 的 ancestry |

本收尾版本的遠端 CI 於推送後另查，以上 M1 的成功不當成收尾版本的 CI 結論。
GitHub Actions 的正式 `check-updates` 未被手動 dispatch；本機 live dry-run 不是遠端 E2E。

### 09-23 live 新訊號分類

重新讀取 [官方 Claude Code CHANGELOG](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)，
UTC `2026-09-22T18:26:32.035164+00:00`，本文 UTF-8 SHA-256
`8bda81336bb640dac39827d6d28367b0d1ad27eba49693265bcc4afa5d5cd2ec`。
兩筆均位於 **2.1.280**，為既有家目錄路徑的修正紀錄，並非新的專案儲存位置：

| 新增至 changelog 的 token | 原文語意與判定 |
| :--- | :--- |
| `~/.claude/session-env` | Windows 清理程序對重定位用 symlink／junction 的處理修正；既有家目錄狀態位置 C-113 的佐證，不新增專案 ignore 規則 |
| `~/.claude/skills/.trash/` | 修正 manifest 列出技能名稱時把技能誤移入回收目錄的行為；既有回收位置 C-108，未推翻預期的同步技能回收政策 |

告警是「新出現在本來源」，不是宣稱全新機制。C-101 的跨來源註記仍保留原始新增訊號。
這兩筆已人工分類並補回帳本；本輪不擴大 M1 的有限 baseline 遷移，changelog entry 交由部署後
正式 monitor 更新。版本參考為 Claude 2.1.280、Antigravity 2.15.1，仍不因 patch 版號單獨告警。

## 交付邊界

本分支集中 PR #17、M1、PR #14 及其 P2 修正，供整體審查；沒有把任何 PR 合併到遠端 main。
原 PR #14 的舊 head 不含本次 P2 修正，應以本分支的整合結果審查。Issue #16 仍須待 main 部署後
正式排程成功，才由恢復流程自動處理。M2／M3 與 C-110 的 runtime 驗證仍是後續計畫。
