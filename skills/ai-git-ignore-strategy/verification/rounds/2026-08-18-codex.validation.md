# 自動追蹤 Phase 6 遠端驗收結果（Codex）

**日期**：2026-08-18

**倉庫**：`ldsAS/dev-ai-skills`

**結論**：通過。failure、recovery、正式 Issue 隔離與 baseline 防護均符合計畫。

## 正式修正

正式修正已直接推送至 `main`：

- `d657390` `fix(ci): 讓監控失敗正確標紅並處理恢復`
- `60c9ecb` `fix(monitor): 降低版本基準線噪音並修正告警語意`
- `45db4bb` `test(docs): 補監控回歸測試與驗證文件`

push 後的唯讀 CI：

- Verify Update Monitor：[run 32109130983](https://github.com/ldsAS/dev-ai-skills/actions/runs/32109130983) — success
- Verify gitignore Template：[run 32109130934](https://github.com/ldsAS/dev-ai-skills/actions/runs/32109130934) — success

## 隔離方式

暫時在 `codex/monitor-validation` 分支加入 `workflow_dispatch.validation_mode`，只允許：

- `failure`：產生 `CHECK_FAILED`，只操作標題為
  `🧪 [VALIDATION] AI 工具異動檢查失敗` 的測試 Issue。
- `recovery`：不產生 signal，只尋找並關閉上述完全同標題的測試 Issue。
- validation dispatch 額外禁止 baseline commit step。

正式異動與失敗 Issue 標題不會被 validation mode 使用。

## Failure 驗收

- Run：[32109290019](https://github.com/ldsAS/dev-ai-skills/actions/runs/32109290019)
- 結果：**failure（預期）**。
- `Run check script` 與 `Notify via GitHub Issues` 成功。
- 最後的 `Fail workflow when checks failed` 明確以 exit 1 將 job 標紅。
- 建立專用 [Issue #9](https://github.com/ldsAS/dev-ai-skills/issues/9)。
- `Commit baseline update` 未執行。

## Recovery 驗收

- Run：[32109353390](https://github.com/ldsAS/dev-ai-skills/actions/runs/32109353390)
- 結果：**success**。
- `Close recovered failure Issues` 成功。
- Issue #9 收到包含 recovery run URL 與 UTC 時間的恢復留言後，自動關閉。
- recovery 後沒有同標題的 open validation Issue。
- `Commit baseline update` 未執行。

## 受保護狀態

驗收前後逐項比對：

| 項目 | 驗收前 | 驗收後 |
| :--- | :--- | :--- |
| 正式 Issue #7 | OPEN；`updatedAt=2026-08-14T01:56:50Z` | 相同 |
| 正式 Issue #8 | OPEN；`updatedAt=2026-08-10T03:19:33Z` | 相同 |
| `scripts/last_checked.json` blob | `0772fb8229d2d864ece95efed9d4c66b88a64bc4` | 相同 |

因此 validation 未留言、關閉或更新正式 Issue #7／#8，也未修改 baseline。

## 清理

- 暫時入口移除 commit：`b7afd95`。
- 移除後 validation 分支與 `main` 的 tree 均為
  `08c14b73bac2abc6a7989bbe86fcaf5ae6f3a9b3`。
- 清理版唯讀 CI：[run 32109530215](https://github.com/ldsAS/dev-ai-skills/actions/runs/32109530215) — success。
- 遠端與本機 `codex/monitor-validation` 分支均已刪除。
- 正式 `main` 從未包含 `validation_mode`，未留下長期測試入口。
